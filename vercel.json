{
    "buildCommand": "npm run build",
    "outputDirectory": ".output",
    "framework": "nuxtjs",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}